# js-reverse worklog

- 时间：2026-04-19
  - 阶段：结合 notion2api 反向优化 js-reverse 知识沉淀
  - 发现或问题：`analysis/notion2api` 暴露出一类高频场景：目标不是单独还原算法，而是从真实请求、初始化接口、localStorage、运行态 store、Cookie 中联合恢复可运行环境；现有 `js-reverse` skill 已有 `notion-env-capture-pattern.md`，但对“多源 env 提取”“登录态半初始化”“usage 接口非实时更新”等经验约束仍不够集中，容易让后续分析停留在单一 Cookie 或单一源码推断层面。
  - 结论：补充一个新的项目级知识文件 `notion-live-evidence-and-multi-source-env.md`，专门沉淀“真实网络优先、多源联合恢复、先证据化再决定交付形态、不要默认 usage 实时更新”等规则；同时更新 `js-reverse.md`、知识库索引、`update-js-reverse.md` 与根 README 的仓库描述，使这类任务有更明确的入口与触发条件。
  - 下一步：继续把 notion2api 中已重复暴露的误判路径收敛到错题本，形成可复用的快速检查项。

- 时间：2026-04-19
  - 阶段：补充 js-reverse 错题本
  - 发现或问题：notion2api 中已有多类可复用误判路径，典型包括“把 Cookie 当完整 env”“用页面推断替代真实网络证据”“把字段缺失误判为字段不存在”“默认 usage 实时更新”“在明确被风控拒绝后仍过早坚持纯 HTTP 方案”；这些问题会导致阶段判断错误或在错误路线长时间停留。
  - 结论：新增 `06-learning/08-mistakes/js-reverse-mistakes.md`，并把错题本目录加入知识库索引，便于后续类似任务快速自检。
  - 下一步：若后续再积累到重复案例，可继续按“现象、错误判断、正确做法、快速检查”格式追加条目。
- 时间：2026-04-19
  - 阶段：开始封装 OpenAI 兼容服务
  - 发现或问题：用户已确认继续，需要在现有独立脚本基础上补一个最小可用的 OpenAI 兼容层，至少覆盖 `/v1/chat/completions` 的非流式与流式输出。
  - 结论：优先做单文件本地 HTTP 服务，直接复用 `perplexity2api.py` 的请求逻辑，避免引入不必要抽象。
  - 下一步：实现最小兼容服务并做本地请求验证。
- 时间：2026-04-19
  - 阶段：结合 perplexity2api 优化 js-reverse skill
  - 发现或问题：`analysis/perplexity2api` 暴露出另一类高频逆向任务：目标并非单点算法还原，而是网页 AI 对话链路的接口分层、SSE 流式、模型映射、线程详情、额度接口与 OpenAI 兼容封装联合分析。现有 `js-reverse` skill 对“AI 对话站点不是默认 WebSocket”“首个接口可用不代表线程详情和额度接口也可用”“应先收敛最小可用接口集再扩兼容范围”“页面 hook 抓不到请求时要尽快转向运行时上下文差异”这些规则强调不足。
  - 结论：更新 `js-reverse.md` 与 `update-js-reverse.md`，把 AI 对话站点专项规则提升为通用强制约束，减少后续在 SSE / 403 / 兼容层范围上的误判和无效盲试。
  - 下一步：如后续再出现同类任务，可继续把“接口分层鉴权”和“运行时复用优先于同层盲试”沉淀为独立知识文件。
- 时间：2026-04-19
  - 阶段：补强 update-js-reverse 流程约束
  - 发现或问题：当前 `update-js-reverse` 已要求读取目标工作目录并回写 skill / 知识库，但没有把“先为对应工作目录生成或更新统一 `analysis.md`”前置成强制步骤，容易导致项目总结沉淀缺失，后续横向复盘材料不完整。
  - 结论：在 `update-js-reverse` 中新增强制前置步骤：先读取目标目录 `README.md`、`WORKLOG.md`、关键文件与已有 `analysis.md`，优先为该目录生成或更新统一分析总结，再继续归类问题与优化 skill / 知识库。
  - 下一步：后续凡执行该 skill，都应先检查目标目录是否已有高质量 `analysis.md`，没有则补齐，有则按新增证据更新。
- 时间：2026-04-19
  - 阶段：继续优化 update-js-reverse 表达形式
  - 发现或问题：虽然此前已加入 `analysis.md` 前置步骤，但整体措辞仍偏说明文，执行顺序和硬性约束还不够像 checklist，后续容易被弱化为“建议”而不是“必须”。
  - 结论：重写 `update-js-reverse.md` 结构，改为“硬性前提、执行目标、强制流程、输出要求、硬性约束”的 checklist 形式，并明确 `analysis.md` 未更新前禁止开始 skill 与知识库优化。
  - 下一步：后续如再发现执行歧义，可继续把各步骤压缩成更短、更强的判定句。

- 时间：2026-04-19
  - 阶段：提交前敏感信息复查
  - 发现或问题：仓库根目录 `.env` 含真实 QQ 邮箱与授权码，`analysis/notion2api/accounts/` 含真实 Notion 凭证导出结构；当前已跟踪改动中未发现用户提到的邮箱、授权码等敏感值，但未跟踪分析目录存在误提交风险。
  - 结论：将 `analysis/notion2api/accounts/` 补充进 `.gitignore`，提交时避免使用 `git add .`，优先使用 `git add -u` 仅纳入已跟踪改动。
  - 下一步：补充忽略规则后再做一次 `git status` 复查，确认无凭证文件进入暂存区。

- 时间：2026-04-19
  - 阶段：提交前敏感信息脱敏
  - 发现或问题：复查待提交内容时发现多份分析文档和工作日志中直接出现测试邮箱域名示例，不符合后续文档脱敏要求，也会增加误提交敏感信息的风险。
  - 结论：统一将文档与脚本中的测试邮箱改写为 `@example.test` 占位形式，并补充仓库规则，明确 WORKLOG.md、README.md、analysis.md 等文档禁止写入真实敏感信息，记录时必须先脱敏或改写为示例结构。
  - 下一步：再次扫描暂存区中的邮箱、授权码、凭证字段和值，确认仅保留占位符、字段结构和必要说明后再提交。
