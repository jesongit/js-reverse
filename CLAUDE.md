请严格遵守以下全部规则，全程不违反任何一条，所有回复、注释、Git 提交信息均纯中文展示：
1. 语言要求：全程使用标准简体中文，禁止出现繁体、乱码及冗余无关内容；技术术语可正常使用英文。
2. 工具约束：禁止使用 [Z.ai](Z.ai) 相关工具，仅基于原生能力完成编码、代码解释、提交信息生成、需求响应等任务。
3. Karpathy 编码准则（强制执行）：
（1）先思考再编码：明确陈述假设，不确定就提问；多种解读全部列出，不私自选定；有更简单方案主动提出，该退回就退回；不清楚则停止并说明困惑。
（2）简洁优先：不加未被要求的功能、抽象、灵活性；不为不可能场景做错误处理；冗余代码必须精简重写。
（3）精准修改：不顺手改进无关代码，不重构正常代码；匹配现有风格，不删无关死代码；仅移除本次改动导致无用的导入、变量、函数。
（4）目标驱动执行：新增功能→添加验证并编写无效输入测试；修复 bug→编写复现测试；重构→确保前后测试均通过。
4. 编码规范（强制执行）：
（1）命名：变量、函数、类等正常使用英文标识符，语义清晰，不使用模糊命名。
（2）注释：全部使用中文，简洁准确，标注关键逻辑；函数与类需说明用途、参数、返回值。
（3）格式：匹配现有代码风格，缩进一致，换行合理；标点符号中文、英文按场景合理使用。
（4）逻辑：清晰简洁，避免冗余嵌套，异常处理仅针对实际可能场景。
5. Git 提交规范（强制执行）：
（1）格式：类型: 中文简洁描述，一行完成，可补充详细中文说明。
（2）类型：feat（新增功能）、fix（修复问题）、docs（文档修改）、style（格式调整）、refactor（重构）、test（测试相关）、chore（配置调整）。
（3）要求：描述纯中文、无多余符号，语义明确，体现提交核心内容。
后续所有回复、代码编写、提交信息生成，均严格遵循以上全部规则，不遗漏任何一条。

注意事项
1. 禁止改动、读取 prompt.md
2. 所有新建或重命名的文件、目录名称必须使用英文，不得使用中文或中英混合命名
3. 处理任何项目任务时，只要出现关键过程、关键发现、关键证据、关键链路、关键请求头或参数结构、重要假设、明确问题、难点、阻塞点、解决方式、阶段结论，必须立刻更新对应工作目录下的 WORKLOG.md，不能等任务结束后再补
4. 若对应工作目录不存在 WORKLOG.md，需先创建再继续记录
5. WORKLOG.md 中的记录必须追加写入，内容需聚焦当前工作目录任务，并至少包含时间、阶段、发现或问题、结论、下一步
6. WORKLOG.md、README.md、analysis.md 及其他文档中禁止写入真实敏感信息，包括但不限于真实邮箱、手机号、授权码、Cookie、token、session、密钥、身份证号、住址等；如需记录，必须先做脱敏或改写为示例值、占位符、结构说明
7. 若变更影响使用方式、安装步骤、命令示例、目录结构、接口或最终行为，仍需同步更新对应工作目录下的 README.md；README.md 只维护稳定说明，不承载滚动工作日志